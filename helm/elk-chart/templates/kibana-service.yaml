apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  ports:
    - port: 5601
      targetPort: 5601
      nodePort: {{ .Values.kibana.nodePort }}
  selector:
    app: kibana